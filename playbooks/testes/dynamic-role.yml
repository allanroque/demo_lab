---
- name: Execução genérica de roles via AAP
  hosts: all
  gather_facts: false 

  tasks:
    - name: DEBUG - Mostrar role_name e variáveis resolvidas
      debug:
        msg:
          - "Role: {{ role_full_path }}"
          - "Variáveis:"
          - "{{ resolved_role_vars }}"
